#include "nullComma.h"

static void class1();

int nullCommaClass() {
    class1();
    return 0;
}

void class1() {
/*
    int i(5,10); // 10 is assigned to i
    int j = (f1(), f2()); // f1 is run, f2 is run, then j is set to return value of f2
    
    while (i < 100)
        sum += data[i], ++i;
    x = (y = 3, (z= ++y +2) + 5);
    
    for(i = 0, j = 100; i!=0; ++i, j-=10)
        ;

    printf("jason"),
        printf("Fedin"),
        printf("Done");
    
    houseprice = 249,500; // typo with values over 1000
    // same as
    house price = 249;
    500;
    
    // not comma operators
    char ch, date;
    int a =1, b= 2;
    printf("%d",x);
    
    void fun(x,y);
*/    
    return;
}